---
sidebar_position: 1
sidebar_label: VKPR devportal install
title: VKPR devportal install
---

# VKPR devportal install

## Descrição

Instala o DevPortal no cluster. Para mais informações sobre o DevPortal, clique [aqui](https://docs.platform.vee.codes/devportal/intro).

## Comando

Comando interativo:

```bash
  vkpr devportal install [flags]
```

Comando não interativo ou utilizando o `VKPR Values`:

```bash
  vkpr devportal install --default
```

## Referência de valores

### Flags

```sh
  --default              Use para preencher automaticamente as entradas com valores padrões
  --dry_run              Simula uma instalação
  --domain               Define o endereço da aplicação
  --secure               Utilizar HTTPS na aplicação
  --provider             Especifica o provedor git para modelos de publicação (Github/Gitlab)
  --git_repo             Especifica o nome do repositório com o catálogo
  --git_branch           Especifica o nome da ramificação do repositório

  --provedor=github
  --client_organization  Especifica sua organização Github
  --github_client_id     Especifica o clientId do provedor de autenticação do Github
  --github_client_secret Especifica o clientSecret do provedor de autenticação Github 
  --github_token         Especifica seu token de acesso pessoal do Github

  --provider=gitlab
  --client_groups        Especifica seus grupos do Gitlab
  --gitlab_token         Especifica seu token de acesso pessoal do Gitlab

```

### Arquivo de configuração

#### Globals

Os seguintes valores são suportados pelo globals:

- `domain` - (Obrigatório) Define o endereço da aplicação. O valor padrão é `localhost`.
- `secure` - (Obrigatório) Define que a aplicação será acessada externamente por HTTPS. O valor padrão é `false`.
- `namespace` - (Opcional) Define o namespace o qual a aplicação será instalada. O valor padrão é `vkpr`.
- `ingressClassName` - (Opcional) Define qual o ingressController que será utilizado para expor a aplicação. O valor padrão é `nginx`.

#### Local

Os seguintes valores são suportados:

- `enabled` - (Opcional) Habilita a instalação se realizada com o comando [vkpr apply](/docs/commands/apply).
- `namespace` - (Opcional) Define o namespace o qual a aplicação será instalada. O valor padrão é `vkpr`.
- `ingressClassName` - (Opcional) Define qual o ingressController que será utilizado para expor a aplicação. O valor padrão é `nginx`.
- `helmArgs` - (Opcional) Opção utilizada para definir valores próprios do chart não suportados pelo comando.

:::caution Cuidado
Utilizar o helmArgs pode fazer com que a aplicação não se comporte como esperado ou ter valores sobrescritos definidos pelo VKPR.
Use-o quando souber o que está fazendo!
:::

### Valores Env

As seguintes variáveis são suportadas:

- `namespace` - **VKPR_ENV_DEVPORTAL_NAMESPACE**
- `ingressClassName` - **VKPR_ENV_DEVPORTAL_INGRESS_CLASS_NAME**

---

:::info Sobre o comando
`Versão do Chart`: 0.9.0 <br/>
`Versão da aplicação`: v0.7.1 <br/>
`Helm Chart`: [veecode-plataform/devportal](https://vfipaas.github.io/public-charts/) <br/>
:::

## Veja também

- [VKPR devportal remove](/docs/commands/devportal/remove) - Desinstala o DevPortal do cluster.
