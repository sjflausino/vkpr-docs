---
sidebar_position: 1
sidebar_label: VKPR devportal install
title: VKPR devportal install
---

# VKPR devportal install

## Description

Install devportal into cluster. For more information about devportal, click [here](https://docs.platform.vee.codes/devportal/intro).

## Commands

Interactive inputs:

```bash
  vkpr devportal install [flags]
```

Non-interactive without setting values or using `VKPR Values`:

```bash
  vkpr devportal install --default
```

## Argument Reference

### Flags

```sh
  --default              Use to automatically fill inputs with default values
  --dry_run              Simulate an install
  --domain               Set the application address
  --secure               Using HTTPS in the application
  --provider             Specifies the git provider for publishing templates (Github/Gitlab)
  --git_repo             Specifies the name of the repository with the catalog
  --git_branch           Specifies the name of the repository branch

  --provider=github
  --client_organization  Specifies your Github organization
  --github_client_id     Specifies your Github auth provider clientId
  --github_client_secret Specifies your Github auth provider clientSecret
  --github_token         Specifies your Github personal access token

  --provider=gitlab
  --client_groups        Specifies yourGitlab groups
  --gitlab_token         Specifies your Gitlab personal access token

```

### Values File

#### Globals

The following arguments are supported by the globals:

- `domain` - (Required) Define the domain used by the application. The default is `localhost`.
- `secure` - (Required) Specifies if the services from the application will expose HTTPS ports inside the cluster. The default is `false`.
- `namespace` - (Optional) Define where the application will be provisioned. The default is `vkpr`.
- `ingressClassName` - (Optional) Define which ingress controller will expose the application. The default is `nginx`.

#### Local

The following arguments are supported:

- `enabled` - (Required unless using the [vkpr apply](/docs/commands/apply)) Enable the auto install from the application.
- `namespace` - (Optional) Define where the application will be provisioned. The default is `vkpr`.
- `ingressClassName` - (Optional) Define which ingress controller will expose the application. The default is `nginx`.
- `helmArgs` - (Optional) Used when you need to change some value or add a new value to the helm values used by the formula.

:::caution
Using helmArgs the application may behave in a way that VKPR may not be used in the best way it was designed.
Use it when you know what you're doing
:::

### Environment values

The following variables are supported:

- `namespace` - **VKPR_ENV_DEVPORTAL_NAMESPACE**
- `ingressClassName` - **VKPR_ENV_DEVPORTAL_INGRESS_CLASS_NAME**

---

:::info About the Formula
`Chart Version`: 0.9.0 <br/>
`Application Version`: v0.7.1 <br/>
`Helm Chart`: [veecode-plataform/devportal](https://vfipaas.github.io/public-charts/) <br/>
:::

## See Also

- [VKPR devportal remove](/docs/commands/devportal/remove) - Uninstall devportal from the cluster.
